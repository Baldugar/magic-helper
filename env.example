# Copy to .env and adjust if needed. docker-compose loads .env automatically.
# Non-standard host ports so you can run this stack alongside a local server (8080) and ArangoDB (8529).

# ArangoDB: host port (container always 8529). Use 8530 to avoid conflict with local ArangoDB on 8529.
ARANGO_HOST_PORT=8530

# App proxy: host port (container always 80). Use 3001 so you can keep e.g. another app on 3000.
PROXY_HOST_PORT=3001
